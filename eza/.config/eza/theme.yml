# ~/.config/eza/theme.yml

# Colors & styles go under these sections:
# filekinds, users, links, date, header, size, git, filenames, extensions, etc.

filekinds:
  directory:         { foreground: "#333333", is_bold: true }
  symlink:           { foreground: "#333333", is_italic: true }
  executable:        { foreground: "#333333" }
  block_device:      { foreground: "#333333" }
  char_device:       { foreground: "#333333" }
  socket:            { foreground: "#333333" }
  pipe:              { foreground: "#333333" }
  special:           { foreground: "#333333" }   # “other_writable” look-alike

users:
  user:              { foreground: "#333333" }   # uid
  group:             { foreground: "#333333" }

links:
  normal:            { foreground: "#333333" }
  broken:            { foreground: "#333333", is_bold: true }

git:
  new:               { foreground: "#333333" }
  modified:          { foreground: "#333333" }
  deleted:           { foreground: "#333333" }
  renamed:           { foreground: "#333333" }

date:
  modified:          { foreground: "#333333" }

header:
  path:              { foreground: "#333333", is_bold: true }

# Optional: per-extension tweaks
extensions:
  md:   { filename: { foreground: "#333333" } }
  log:  { filename: { foreground: "#333333" } }
  zip:  { filename: { foreground: "#333333" } }
